diff --git a/node_modules/@sun-asterisk/sunlint/engines/eslint-engine.js b/node_modules/@sun-asterisk/sunlint/engines/eslint-engine.js
index 83dc74a..17435cf 100644
--- a/node_modules/@sun-asterisk/sunlint/engines/eslint-engine.js
+++ b/node_modules/@sun-asterisk/sunlint/engines/eslint-engine.js
@@ -9,6 +9,7 @@ const AnalysisEngineInterface = require('../core/interfaces/analysis-engine.inte
 const dependencyChecker = require('../core/dependency-checker');
 const fs = require('fs');
 const path = require('path');
+const { pathToFileURL } = require('url');
 const { getInstance } = require('../core/unified-rule-registry');
 
 class ESLintEngine extends AnalysisEngineInterface {
@@ -397,7 +398,7 @@ class ESLintEngine extends AnalysisEngineInterface {
       };
       
       // Create temporary config file in project directory
-      const tempConfigPath = path.join(projectPath, '.sunlint-eslint.config.js');
+      const tempConfigPath = path.join(projectPath, '.sunlint-eslint.config.mjs');
       
       // Create simple config compatible with flat config format
       const configForExport = {
@@ -415,7 +416,7 @@ class ESLintEngine extends AnalysisEngineInterface {
       };
       
       const configContent = `// Temporary flat config generated by SunLint
-import customPlugin from '${path.resolve(__dirname, '../integrations/eslint/plugin/index.js')}';${pluginImports}
+import customPlugin from '${pathToFileURL(path.resolve(__dirname, '../integrations/eslint/plugin/index.js')).href}';${pluginImports}
 
 export default [
   ${JSON.stringify(configForExport, null, 2).replace('"rules":', `"plugins": ${pluginDefs},\n    "rules":`)},
